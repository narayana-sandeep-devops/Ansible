- name: varialble preferrence
  hosts: local
  connection: local
  # vars:
  #   NAME: "Ansible from Play"
  vars_files:
  - course.yml
  # vars_prompt:
  # - name: NAME
  #   prompt: please enter name
  #   private: false
  tasks:
  - name: print name
    # vars:
    #   NAME: "Ansible from task"
    ansible.builtin.debug:
      msg: "Hi {{ NAME }}"