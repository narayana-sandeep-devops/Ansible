- name: Install and Run Nginx
  hosts: web
  become: true
  tasks:
  - name: Install Nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Start Nginx Service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: true