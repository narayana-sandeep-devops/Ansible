- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print loops
    ansible.builtin.debug:
      msg: " Hello {{ item }}" #item is registered keyword
    loop:
    - SANDEEP
    - NANDINI
    - VINYA